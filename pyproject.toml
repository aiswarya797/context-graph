[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "context-graph-benchmark-method"
version = "0.1.0"
description = "Plain Codex SWE-Explore baseline apparatus"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["benchmark_method/codex_baseline/src"]
testpaths = ["benchmark_method/codex_baseline/tests"]
